<?php
    // Build in Functions

    /* Date
    untuk menampilkan tangan dengan format tertentu
    echo date("l"); */

    /* Time
    UNIX Timestamp / EPOCH time
    detik yang sudah berlalu sejak 1 Januari 1970
    echo time(); */
    // echo date("l", time()-60*60*24*100);

    /* mktime
    membuat sendiri detik
    mktime(0,0,0,0,0,0)
    jam, menit, detik, bulan, tanggal, tahun
    echo date("l", mktime(0,0,0,27,12,2002)); */

    /* strtotime
    echo date("l", strtotime("29 des 2002")); */

    /* String
    strlen()
    strcmp()
    explode()
    htmlspecialchars() */

    /* Utility
    var_dump()
    isset()
    empty()
    die()
    sleep() */
?>